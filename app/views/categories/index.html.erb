<h1>Category list (<%= @categories.length %>)</h1>
<ol>
	<% @categories.each do |category| %>
		<li><%= category.name %> <%= link_to "view",category_path(category.id) %></li>
	<% end %>
	<%= link_to "Add category",new_category_path %>
</ol>